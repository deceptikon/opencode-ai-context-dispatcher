# 🚀 Backend Onboarding Prompt Template

Use this prompt to run a comprehensive onboarding session for any Backend project (API, Services, Data Processing).

## Usage

```bash
# 1. Get your project ID
ctx list

# 2. Run onboarding with the prompt below
ocx -c full <project-id> "[paste the prompt below]"
```

## Backend Onboarding Prompt

```text
CRITICAL ONBOARDING SESSION - Backend Specialization

Your task is to thoroughly explore and document how to work with this BACKEND project.
You will save everything you learn so future agents can be productive immediately.

IMPORTANT: Use 'run' bash tool to explore. Test commands, don't just read about them.

PHASE 1: INFRASTRUCTURE & STACK (20 min)
========================================

1. Identify the Language & Core Framework
   run ls -la | grep -E "package.json|pyproject.toml|requirements.txt|go.mod|Cargo.toml|Gemfile|build.gradle"
   run cat package.json 2>/dev/null | grep -E "express|nest|fastify|koa"
   run cat pyproject.toml 2>/dev/null | grep -E "django|fastapi|flask"
   run cat go.mod 2>/dev/null | head -n 10

2. Environment & Config
   run ls -la .env* config/ settings/ 2>/dev/null
   run cat README.md | grep -i "environment" -A 10

3. Database & Storage
   run cat README.md | grep -E "Postgres|MySQL|Redis|MongoDB|SQL" -i
   run find . -name "*docker-compose*" -o -name "Dockerfile"

PHASE 2: API & BUSINESS LOGIC (20 min)
=======================================

1. Endpoint Discovery
   run find . -name "*routes*" -o -name "*controller*" -o -name "*views.py*" -o -name "*api*" | head -20
   run grep -r "router" . | head -n 10

2. Data Models
   run find . -name "*models*" -o -name "*entities*" -o -name "*schema*" | head -20

3. Authentication & Security
   run grep -rE "jwt|auth|middleware|permission|guard" . | head -n 20

PHASE 3: COMMAND DISCOVERY (20 min)
===================================

Test and document the EXACT commands:

1. Local Execution
   run npm run dev 2>&1 | head -n 10
   run python manage.py runserver 2>&1 | head -n 10
   run go run main.go 2>&1 | head -n 10

2. Testing Suite
   run npm test -- --help 2>&1 | head -n 5
   run pytest --collect-only 2>&1 | head -n 10
   run go test ./... -v | head -n 10

3. Database Migrations
   run python manage.py showmigrations 2>/dev/null
   run npm run migrate --help 2>/dev/null

PHASE 4: DOCUMENTATION (30 min)
================================

Save your findings using 'ctx add-doc <project-id>'.

RULES (How to run):
- Rule for Testing: Exact command to run unit/integration tests.
- Rule for Linting/Formatting: Commands to keep code clean.
- Rule for Database: How to run migrations or seed data.
- Rule for Local Dev: How to start the application with hot-reload.

DOCS (What things are):
- Tech Stack: List languages, frameworks, DBs, and key libraries.
- Directory Structure: Explain where models, controllers, and services live.
- Architecture: Describe if it's Monolithic, Microservices, Clean Architecture, etc.

NOTES (Workflows):
- Onboarding Checklist: Steps for a new developer to get started.
- API Patterns: How to add a new endpoint (controller -> service -> model).
- Troubleshooting: Common errors (e.g., 'DB connection failed') and fixes.
- PR Standards: What to check before submitting code.

PHASE 5: VERIFICATION
=====================

run ctx list-docs <project-id>
Verify you have at least:
- 4 Rules
- 3 Docs
- 4 Notes
```

---
*Generated by OpenCode Context Dispatcher*
